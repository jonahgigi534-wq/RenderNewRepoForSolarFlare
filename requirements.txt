# Core ML / data
# Pinned to the versions the shipped models were trained with, so the reported
# TSS scores reproduce on a clean install. Loosen to >= if you want newer wheels.
numpy==2.5.0
pandas==3.0.3
scikit-learn==1.9.0
joblib==1.5.3
pyyaml>=6.0

# Live data + API
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29
drms>=0.7          # JSOC SHARP fetches (sharpdata, live inference, dataset builds)
scipy>=1.11        # KS tests (research/exp2, experiments.gap_diagnosis)

# Dataset download (Google Drive)
gdown>=5.1

# Experiments (STEP 7): publication-quality "skill vs lead time" figure
matplotlib>=3.8

# Optional gradient-boosting candidates for the model bake-off. train.py adds
# them only if importable; without them the bake-off runs the 4 sklearn models.
lightgbm>=4.0
xgboost>=2.0
