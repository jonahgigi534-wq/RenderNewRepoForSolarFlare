__pycache__/
*.pyc
.cache/
data/swansf/*.pkl
data/swansf/swansf_download
data/sharp_live/
data/swansf_full/
*.npz
*.tar.gz
# Trained model artifacts are shipped via Git LFS (see .gitattributes) so the
# deploy gets them. The .pkl duplicates are redundant (the app loads .joblib).
models/*.pkl
.venv/
.DS_Store
*.log
.claude/launch.json
.claude/settings.local.json
.claude/auto-commit.ps1
models.zip
*.zip
